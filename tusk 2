# 2. Для списка реализовать обмен значений соседних элементов, т.е. Значениями обмениваются
# элементы с индексами 0 и 1, 2 и 3 и т.д. При нечетном количестве элементов последний
# сохранить на своем месте. Для заполнения списка элементов необходимо использовать функцию
# input().

# list использовал,на случай ввода данных сплошным текстом. В split не понял, как сделать правильный
# вывод списка, если данные введены сплошным тексом. Т.е., если split, и ввести 123 вместо 1 2 3,
# формируется список ['123'], а не ['1', '2', '3' ]


new_list = list(input("Введите данные списка:"))
for el in range(1, len(new_list), 2):
    new_list[el], new_list[el-1] = new_list[el-1], new_list[el]
print(new_list)
